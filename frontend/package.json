{
  "name": "braille-translator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "setup": "node scripts/install.mjs",
    "setup:update": "node scripts/install.mjs --update",
    "predev": "node scripts/install.mjs --verify-only",
    "dev": "vite",
    "prebuild": "node scripts/install.mjs --verify-only",
    "build": "vue-tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.5",
    "pinia": "^2.1.7"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.2",
    "typescript": "^5.3.3",
    "vite": "^5.0.8",
    "vue-tsc": "^2.2.0",
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.32",
    "autoprefixer": "^10.4.16"
  }
}
